/*
Name : Vaishnavi Mali
Date : 28-8-2019
Program : Creating functions for string oprations like find and replace 
*/

#include<stdio.h>
#include<string.h>
int main(void)
{
    char str[50]="this is a good sweet biscuit";
    char findstr[10],replacestr[10];
    int i,j,count=0;
    printf("\nstring is:-%s",str);
    printf("\nenter the string u want to find:\t");
    scanf("%s",findstr);
    printf("\n enter the string u want to replace:\t");
    scanf("%s",replacestr);

for(i=0;i!='\0';i++)
{
    printf("%s",str);
    
}
printf("string length=%d",i);
    return 0;
}

/// Function to replace string in stirng
/// input parametes  
/// output 
char[] replaceStringInString( char[] input_string, char[] string_to_replace ){
    // Work in progress
}